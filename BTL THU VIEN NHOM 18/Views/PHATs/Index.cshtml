@model IEnumerable<BTL_THU_VIEN_NHOM_18.Models.PHAT>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.mssach)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.msdocgia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.msnhanvien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.lydophat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.mssach)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.msdocgia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.msnhanvien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.lydophat)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
